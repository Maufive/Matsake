$(document).ready(function(){
  $('.anställda-carousel').slick({
    dots: true,
    infinite: true,
    autoplaySpeed: 3000,
    slidesToShow: 1,
    centerMode: false,
    variableWidth: true,
    autoplay: true
  });
});
